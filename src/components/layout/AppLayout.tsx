import React from "react";
import App from "../../App";
// import AuthContextProvider from "../contexts/AuthContext";

const AppLayout = () => {
  return (
    // <AuthContextProvider>
    <App />
    // </AuthContextProvider>
  );
};

export default AppLayout;
